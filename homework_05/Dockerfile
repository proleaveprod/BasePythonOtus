# Сборка образа:
# $ docker build -t homeword05 .
# 
# Запуск контейнера:
# $ docker run 


#Базовый образ, который мы берем за основу
FROM python:3.11-slim

#Указание базовой директории, на которой будет всё крутиться у контейнера. То есть как раз сюда надо будет копировать код и тд и тп
WORKDIR /app

# Если я буду менять Dockerfile внизу, то это уже не будет пересобираться. т.к. при обновлении Dockerfile будет делаться то, что изменлиось, то есть снизу. Поэтому лучше базу так добавлять сразу
COPY requirements.txt .

#Запуск каких-то скриптов и тд, например можно установить зависимости (тут установлю colorama). Их можно делать сколько угодно
RUN pip install --no-cache-dir -r requirements.txt 

COPY . . 

# Указываем четкий порт приложения (в итоге я не понял нахера он вообще нужен, если всё равно он указывается в app.py, и при docker run)
EXPOSE 8000

# То, что будет происходить при запуске docker run
RUN ["python3","app.py"]